# Lock the lockfile version to prevent automatic upgrades
lockfile-version=3

# Use exact versions when installing packages
save-exact=true

# Ensure consistent dependency resolution
legacy-peer-deps=false
